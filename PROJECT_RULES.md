# Project Development Rules

## Development Server

- **Port**: Always use port `8080` for the development server.
- **Command**: `npm run dev` should serve on `http://localhost:8080`.

## Configuration

- This is enforced in `vite.config.js`.
